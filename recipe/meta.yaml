{% set name = "lb-telemetry" %}
{% set version = "0.2.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/lb-telemetry-{{ version }}.tar.gz
  sha256: df2c2d460a8556331dac77ebf3be743bc4e1b98a4664c2bb814292e108e9cc44

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - setuptools >=61
    - wheel
    - setuptools-scm >=3.4
    - pip
  run:
    - python >=3.6
    - lbplatformutils
    - logzero
    - requests

test:
  imports:
    - lb_telemetry
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://gitlab.cern.ch/lhcb/lb-telemetry
  summary: A utility for logging telemetry data from LHCb packages to MONIT
  license: GPL-3.0-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - chrisburr
    - CameronMcClymont
